{
  "name": "mongodb-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "A read-only Model Context Protocol (MCP) server for querying MongoDB databases",
  "main": "dist/index.js",
  "bin": {
    "mongodb-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "mongodb", "model-context-protocol", "database", "read-only"],
  "author": "sanjeev4523",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "mongodb": "^7.1.0",
    "zod": "^4.3.6"
  }
}
